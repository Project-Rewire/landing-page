import { CSSProperties } from "react";

export function GetItOnPlayStoreButton({ styles, disabled }: { styles?: CSSProperties; disabled?: boolean }) {
    return (
        <>
            {/* Implement Google PlayStore Button */}
        </>
    );
}
